import com.asual.enflash.EnFlashObject;

/**
 * LoadManager class will simplify the loading of assets, components, 
 * collections and xml files. Still has to be implemented.
 */
class com.asual.enflash.managers.LoadManager extends EnFlashObject {

	private var _name:String = "LoadManager";

	/**
	 * @param id (optional) Descriptive ID of this object.
	 */
	public function LoadManager(id:String) {
		super(id);
	}
}
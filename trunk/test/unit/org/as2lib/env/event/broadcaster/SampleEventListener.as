import org.as2lib.core.BasicClass;
import org.as2lib.env.event.EventListener;
import org.as2lib.env.event.broadcaster.EventInfo;

class org.as2lib.env.event.broadcaster.SampleEventListener extends BasicClass implements EventListener {
	
	public function onTest(info:EventInfo) {
	}
	
}
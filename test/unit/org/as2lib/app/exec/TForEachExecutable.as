import org.as2lib.app.exec.TExecutable;
import org.as2lib.app.exec.ForEachExecutable;

class org.as2lib.app.exec.TForEachExecutable extends TExecutable {
	public static function blockCollecting(Void):Boolean {
		return true;
	}
	
	public function forEach(inst:ForEachExecutable) {
		
	}
}
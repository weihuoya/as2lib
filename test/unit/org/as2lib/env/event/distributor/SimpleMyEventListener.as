import org.as2lib.core.BasicClass;
import org.as2lib.env.event.distributor.MyEventListener;

class org.as2lib.env.event.distributor.SimpleMyEventListener extends BasicClass implements MyEventListener {
	
	public function onTest(arg1:String, arg2:Object, arg3:Number) {
	}
	
}